const SaveAttendance = () =>{    const AttendanceData = [];    document.getElementById("SaveAttendance").addEventListener("click",() =>{    const AttendanceDate = document.getElementById("Data_Time");        document.querySelectorAll('input[type="checkbox"]').forEach((checkbox) =>{            const studentId = checkbox.value;            const attended = checkbox.checked;            if(attended && AttendanceDate.value !== ""){                AttendanceData.push({studentId: studentId, AttendanceDate: AttendanceDate.value})            }        })        //AJAX REQUEST        $.ajax({            url: "AttendanceRecord.php",            type: "post",            data:{                AttendanceData: AttendanceData,            },            success: function (data,status){                if(status === "success"){                    Swal.fire({                        position: "top-center",                        icon: "success",                        title: "Successfully Saved Your Attendance!",                        showConfirmButton: false,                        timer: 1500                    })                    AttendanceDate.value = ""                }else{                    Swal.fire({                        position: "top-center",                        icon: "error",                        title: status,                        showConfirmButton: false,                        timer: 1500                    })                }            }        })    })}SaveAttendance()