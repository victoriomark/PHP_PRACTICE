const Cart = () =>{    const AddCart  = document.querySelectorAll('#btnOrder');    AddCart.forEach((btn,index) =>{        btn.addEventListener('click',() =>{            // ajax req            $.ajax({                type: 'post',                url: '../Model/Order.php',                data:{                    Id: btn.value                },                success: function (data,status){                    console.log(status)                    const order = document.querySelector('.order');                    order.innerHTML =data                }            })        })    })}Cart()const Order =() =>{    document.addEventListener('click',(event) =>{        if (event.target.id === 'order_na'){            const full_name = document.getElementById('full_name');            const contact = document.getElementById('contact');            const email = document.getElementById('email');            const address = document.getElementById('address');            const qty = document.getElementById('qty');            const Price = document.getElementById('Price')            const ProductName = document.getElementById('ProductName');            if(!full_name.value || !contact.value || !address.value || !qty.value  || !qty.value  ){                alert('Please Input your Order info')            }else {                // ajax                $.ajax({                    type: 'post',                    url: '../Model/AddOrders.php',                    data:{                        full_name: full_name.value,                        contact: contact.value,                        email: email.value,                        address: address.value,                        OrderId: event.target.value,                        qty: qty.value,                        Price: Price.value,                        ProductName: ProductName.textContent                    },                    success: function (data,status){                        alert('Successfully Submit or Order')                    }                })            }        }    })}Order();