const AddingNewProduct = () =>{    let ImageProduct;    document.addEventListener('click',(event) =>{        const ProductImage = document.getElementById('ProductImage')        if(ProductImage !== null){            ProductImage.addEventListener('change',(image) =>{                ImageProduct = image.target.files[0];            })        }        if(event.target.id === 'Add_btn'){            const ProductName = document.getElementById('ProductName');            const Description = document.getElementById('Description');            const ProductPrice = document.getElementById('ProductPrice');            const formData = new FormData();            formData.append('ProductName',ProductName.value);            formData.append('Description',Description.value);            formData.append('ProductPrice',ProductPrice.value);            formData.append('ImageProduct',ImageProduct);            if(!ProductName.value || !Description.value || !ProductPrice.value || !ImageProduct){          alert( "Please Provide Product Info")            }else {                // Ajax Request                $.ajax({                    url: '../Model/AddProduct.php',                    type: 'post',                    data: formData,                    processData: false,                    contentType: false,                    success: function (data,status){                        if(status === 'success'){                        alert("Successfully Added New Product")                        }                        //clear the inputs                        ProductPrice.value = ''                        ImageProduct = ''                        Description.vaue = ''                    }                })            }        }    })}AddingNewProduct()