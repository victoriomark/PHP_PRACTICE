const UpdateDocs = ()  =>{    const status = document.getElementById('Status_up');   document.addEventListener('click',(event) =>{       if (event.target.id === 'btn_update_docs'){           $.ajax({               url: "../Model/UpdateReqestedDocs.php",               type: "post",               data:{                   Id: event.target.value,               },               success: function (data,status){                  const modal_container = document.getElementById('modal_container');                  modal_container.innerHTML = data;                  $('#Update_docs').modal('show');               }           })       }   })}UpdateDocs()const SaveUpdated_Status = () =>{    let StatusUpdated = "";    document.addEventListener('click',(event) =>{        const Status_UpdateValue = document.getElementById('Status_UpdateValue');        if(Status_UpdateValue){               Status_UpdateValue.addEventListener('change',(event) =>{                   StatusUpdated =  event.target.value;               })           }        if(event.target.id === 'save_btn'){          console.log(StatusUpdated)            $.ajax({                url: "../Model/SaveDocsUpdated.php",                type: "post",                data:{                    SaveId: event.target.value,                    StatusUpdated: StatusUpdated                },                success: function (data,status){                   if(status === 'success'){                       Swal.fire({                           title: "Successfully Updated",                           icon: "success",                           confirmButtonColor: '#3FC495'                       });                       //try                       setTimeout(()=>{                         window.location.reload()                       },3000)                   }                }            })        }    })}SaveUpdated_Status()